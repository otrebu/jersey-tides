# Vocabulary

Naming conventions for consistency across the codebase.

| Context | Preferred | Avoid |
|---------|-----------|-------|
| HTTP server instance | `server` | `app` |
| Server factory function | `createServer()` | `buildApp()`, `buildServer()` |
