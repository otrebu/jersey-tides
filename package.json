{
  "name": "jersey-tides",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "packageManager": "pnpm@10.26.2",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r --filter '!fit-tools' exec tsc --noEmit",
    "test": "pnpm -r test",
    "dev": "pnpm --filter demo dev",
    "fixtures": "node tools/fit/gen-fixtures.mjs",
    "refit:check": "node tools/fit/verify-engine-move.mjs",
    "changeset": "changeset",
    "release:publish": "pnpm -r publish --access public --no-git-checks --provenance"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0"
  }
}
