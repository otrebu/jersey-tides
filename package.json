{
  "name": "jersey-tides",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "packageManager": "pnpm@10.26.2",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r --parallel exec tsc --noEmit",
    "test": "pnpm -r test",
    "dev": "pnpm --filter demo dev"
  }
}
